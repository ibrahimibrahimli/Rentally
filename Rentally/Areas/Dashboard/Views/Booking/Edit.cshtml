@model Booking

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @* <label asp-for="Status" class="control-label">Kateqoriya</label>
                <select asp-for="Status" class="form-control">
                    @foreach (var item in ViewData["CarCategories"] as IEnumerable<CarCategory>)
                    {
                        <option value="@item.Id">@item.Title</option>
                    }
                </select> *@

                <input type="text" id="2" value="Təsdiqlə" class="btn btn-primary" />
                <input type="text" id="3" value="Ləğv et" class="btn btn-primary" />
            
            <div class="form-group">
                <input type="submit" value="@DefaultConstantValue.ADD_MESSAGE" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">@DefaultConstantValue.RETURN_TO_BACK</a>
</div>
